(ns scterm.requests.views)

(defn requests-view []
  [:box#requests [:box#logs {:align :center
                             :valign :middle
                             :content "This is the requests page. Under construction!"}]])
